name: Pull Request

on:
  pull_request:
    paths-ignore:
      - '*.md'
      - '*.png'
      - '.gitignore'

jobs:
  # lint:
  #   uses: ./.github/workflows/lint.yml

  sgn:
    uses: ./.github/workflows/sgn.yml

  treeimprovementbase:
    uses: ./.github/workflows/treeimprovementbase.yml
